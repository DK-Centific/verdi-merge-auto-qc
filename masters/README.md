# Local Vendor Prod Files (demo only)

Drop a **copy** of `Vendor Prod Files.xlsx` here if you want Send to work without SharePoint.

- The live destination of truth is the SharePoint Rate Approval Log (see repo README).
- Do not commit real workbooks. This folder is gitignored except this note.
- On Render the disk is ephemeral — local writes here are not production.
